#ifndef EVAL
#define EVAL
#include <vector>
using namespace std;

int eval(vector<int> &solution,  vector<vector<int>> &options, vector<int> &blockSize, vector<int> &carsBlock, int num_vehicles, int num_options);

#endif